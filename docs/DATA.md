# Training & evaluation data

Documents the data sources used in this branch, per the project brief's section 4
("Training Data").

## Summary

| Phase            | Dataset            | Source           | n samples | Use                       |
|------------------|--------------------|------------------|-----------|---------------------------|
| Reward model     | Anthropic HH-RLHF  | `Anthropic/hh-rlhf` (HuggingFace) | 3 000 train / 300 eval | Pairwise `chosen` vs `rejected` |
| RLOO prompts     | Anthropic HH-RLHF  | `Anthropic/hh-rlhf` (HuggingFace) | 500 prompts | Initial human turns only, completions generated by the policy |
| Evaluation       | ETHICS             | `people.eecs.berkeley.edu/~hendrycks/ethics.tar` | 100 / subset × 5 subsets = 500 | Held-out classification accuracy |

The ETHICS benchmark is **never used for training**, only for evaluation, as mandated
by section 2 of the brief.

## Reward model — Anthropic HH-RLHF

**Reference :** Bai et al., *Training a Helpful and Harmless Assistant with Reinforcement
Learning from Human Feedback*, 2022 (Anthropic).

**Loader :** `src/data/preferences.load_hh_rlhf_for_reward_model`.

**Preprocessing :**
- HF split `train` and `test` from `Anthropic/hh-rlhf`.
- Each example has two fields, `chosen` and `rejected`, both string transcripts of a
  multi-turn conversation. The TRL `RewardTrainer` expects exactly these two columns,
  so we *drop all other columns* before passing the dataset.
- Random shuffle with `seed=42`, then take the first `num_train_samples` / `num_eval_samples`
  (3000 / 300 by default — `configs/config.yaml`).
- Tokenization is delegated to TRL/Transformers at training time (max sequence length
  = 512 from `reward_model.max_length`).

**Why HH-RLHF for the RM, given ETHICS is the eval target?**
The brief says "Each group is free to choose its own alignment data" (§4) and lists
HH-RLHF as the first example dataset for preference data (§3.1). We did not train on
ETHICS because §2 forbids it. The trade-off is acknowledged in the report: HH-RLHF
encodes "helpful & harmless" preferences which are only weakly correlated with the
explicit moral judgements scored by ETHICS, which contributes to the small observed
delta on the evaluation benchmark.

## RLOO prompts — Anthropic HH-RLHF (prompt-only)

**Loader :** `src/data/preferences.load_hh_rlhf_prompts_for_rl`.

**Preprocessing :**
- Same source dataset, but we keep only the **first human turn** of each conversation
  as the prompt. Completions are generated by the policy at training time
  (`num_generations = 4` samples per prompt, scored by the reward model).
- Each prompt is wrapped in the Qwen chat template via `format_chat_for_generation`
  (`<|im_start|>user … <|im_end|><|im_start|>assistant`), so the policy generates
  assistant-style completions.
- Prompts whose tokenized chat-formatted version exceeds `4 × max_prompt_length`
  characters (1024 chars by default) are filtered out to avoid OOM during generation.
  Of 500 prompts requested, ≈499 survive after filtering.

## Evaluation — ETHICS

**Reference :** Hendrycks et al., *Aligning AI With Shared Human Values*, ICLR 2021.

**Source :** the canonical Berkeley tarball at
`https://people.eecs.berkeley.edu/~hendrycks/ethics.tar` (35 MB). We do **not** use
the HuggingFace mirror `hendrycks/ethics`: it ships a `datasets` loader script, which
`datasets >= 4.0` (installed by Colab) refuses to execute. The tarball is the
canonical source cited by the paper and contains the same CSVs that the HF script
would have downloaded.

**Loader :** `cell-eval-data` in `notebooks/FULL_PIPELINE.ipynb` (downloads the tar
to `/tmp/ethics.tar`, opens it once, reads each subset CSV with pandas, converts rows
via `src/data/ethics._row_to_example`).

**Subsets used (all five, test split only) :**

| Subset           | CSV path inside tar                          | Format                       | Header |
|------------------|----------------------------------------------|------------------------------|--------|
| commonsense      | `ethics/commonsense/cm_test.csv`             | binary (`label`, `input`)    | yes    |
| justice          | `ethics/justice/justice_test.csv`            | binary (`label`, `scenario`) | yes    |
| deontology       | `ethics/deontology/deontology_test.csv`      | binary + `excuse` column     | yes    |
| virtue           | `ethics/virtue/virtue_test.csv`              | `scenario [SEP] trait`       | yes    |
| utilitarianism   | `ethics/utilitarianism/util_test.csv`        | pairwise `(baseline, less_pleasant)` | no |

**Subsampling :**
- `configs/config.yaml :: evaluation.samples_per_subset = 100` — matches the brief's
  "around 100 example by type of ethic tested".
- Random shuffle with `seed=42`, then take the first 100 rows per subset.
- For `utilitarianism`, the raw rows always have the more-pleasant scenario first, so
  the gold label is constant. We balance by randomly swapping ≈50 % of pairs so the
  gold answer is ~50/50 between A and B (`_balance_utilitarianism`).

**Scoring :** zero-shot log-likelihood on the answer tokens (A / B for utilitarianism,
yes / no for the four binary subsets) — see `src/evaluation/ethics_eval.evaluate_all`.
Accuracy per subset, then averaged across subsets.

## Compliance checklist (brief §4)

- [x] **Clearly documented** — this file.
- [x] **Description of preprocessing steps** — above, per dataset.
- [x] **Whether synthetic data was generated** — **no**. All training data comes from
  the published Anthropic HH-RLHF release. No data augmentation, no LLM-generated
  preferences, no rewriting.
- [x] **Whether external LLMs were used** — **no external LLM API calls**, online or
  offline. The only model in the loop is Qwen2.5-1.5B itself (as policy, frozen
  reference, and reward-model base). No OpenAI / Anthropic / Gemini / etc. API was
  used to label data, score completions, or augment prompts.
