# ────────────────────────────────────────────────────────────────────────────
# Qwen2.5-1.5B-Instruct via transformers (CPU or GPU; fp32 by default)
# ────────────────────────────────────────────────────────────────────────────

# Core ML stack
torch==2.3.0
transformers==4.44.2
accelerate==0.33.0

# Data + datasets
datasets==2.21.0
huggingface_hub>=0.23.0
pandas==2.2.2

# Retrieval (RAG)
sentence-transformers==3.0.1
faiss-cpu==1.8.0
numpy>=1.24

# Plotting + utils
matplotlib==3.8.4
tqdm>=4.66
