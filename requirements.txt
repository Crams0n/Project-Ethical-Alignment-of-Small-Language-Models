# Core ML stack (tested on Python 3.10, CUDA 12.1)
torch>=2.3.0,<2.6.0
transformers>=4.45.0,<4.50.0
accelerate>=0.34.0
datasets>=3.0.0
tokenizers>=0.20.0

# Parameter-efficient fine-tuning
peft>=0.13.0

# RLHF: reward model + PPO
# We pin to a TRL version that still exposes the classical PPOTrainer loop.
trl>=0.11.0,<0.12.0

# Quantization (optional, for QLoRA)
bitsandbytes>=0.43.0

# Utilities
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Notebook helpers (only needed when running notebooks locally)
ipywidgets>=8.1.0
jupyter>=1.0.0

# Experiment tracking (optional — wandb auto-disabled if not logged in)
wandb>=0.18.0
