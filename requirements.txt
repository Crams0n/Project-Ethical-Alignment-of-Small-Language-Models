# Pins that match the `fise-ml-global` venv used for this project.
# Python 3.11, CUDA 12.x. Major-version pins prevent silent breakage when TRL or
# transformers ship another big API rewrite.

torch>=2.6,<3.0
transformers>=5.0,<6.0
accelerate>=1.0,<2.0
datasets>=3.0,<5.0
tokenizers>=0.20,<0.24

# Parameter-efficient fine-tuning
peft>=0.13,<1.0

# RLHF: reward model + RLOO (PPOTrainer was removed in TRL 1.x — we use RLOO, which
# the project brief explicitly authorises as a "lighter alternative to PPO").
trl>=1.0,<2.0

# 4-bit quantization (QLoRA)
bitsandbytes>=0.43

# Utilities
pyyaml>=6.0
tqdm>=4.66
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13

# Notebook helpers
ipywidgets>=8.0
jupyter>=1.0

# Optional experiment tracking
wandb>=0.18
